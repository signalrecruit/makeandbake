              <div class="row">
                  <div class="col-lg-12">
                      <section class="panel">
                          <header class="panel-heading">
                              All <%= pluralize(@admins.count, "admin") %>
                          </header>
                          
                          <table class="table table-striped table-advance table-hover">
                           <tbody>
                              <tr>
                                 <th><i class="fa fa-admin" aria-hidden="true"></i>
 Full Name</th>
                                 <th><i class="fa fa-envelope" aria-hidden="true"></i>
</i> Email</th>
                                 <th><i class="fa fa-mobile" aria-hidden="true"></i>
Phonenumber</th>
                                 <th><i class="icon_cogs"></i> Actions</th>
                              </tr>

                              <% @admins.each do |admin| %>
                              <tr>
                                 <td><%= link_to "#{admin.first_name}, #{admin.last_name}", [:admin, admin] %></td>
                                 <td><%= admin.email %></td>
                                 <td><%= admin.phonenumber %></td>
                                 <td>
                                  <div class="btn-group">
                                      <%= link_to fa_icon("pencil") + " Edit", [:edit, :admin, admin], class: "buy_now" %> <%= link_to "revoke admin rights", revoke_admin_user_path(admin), method: :patch, class: "buy_now" if current_user.super_admin? %> <%#= link_to fa_icon("archive") + " suspend account", suspend_user_account_admin_user_path(admin), data: {confirm: "Are you sure you want to suspend #{admin.first_name}'s account?"}, method: :patch, class: "buy_now" %>
                                  </div>
                                  </td>
                              </tr>
                              <% end %>
                              
                                                            
                           </tbody>
                        </table>
                      </section>
                  </div>
              </div>
              <!-- page end-->


                            <div class="row">
                  <div class="col-lg-12">
                      <section class="panel">
                          <header class="panel-heading">
                              All <%= pluralize(@revoked_admins.count, "revoked admin") %>
                          </header>
                          
                          <table class="table table-striped table-advance table-hover">
                           <tbody>
                              <tr>
                                 <th><i class="fa fa-admin" aria-hidden="true"></i>
 Full Name</th>
                                 <th><i class="fa fa-envelope" aria-hidden="true"></i>
</i> Email</th>
                                 <th><i class="fa fa-mobile" aria-hidden="true"></i>
Phonenumber</th>
                                 <th><i class="icon_cogs"></i> Actions</th>
                              </tr>

                              <% @revoked_admins.each do |admin| %>
                              <tr>
                                 <td><%= link_to "#{admin.first_name}, #{admin.last_name}", [:admin, admin] %></td>
                                 <td><%= admin.email %></td>
                                 <td><%= admin.phonenumber %></td>
                                 <td>
                                  <div class="btn-group">
                                      <%= link_to fa_icon("pencil") + " Edit", [:edit, :admin, admin], class: "buy_now" %> <%= link_to fa_icon("undo") + " restore admin rights", switch_admin_user_path(admin), method: :patch, class: "buy_now" if current_user.super_admin? %> <%#= link_to fa_icon("archive") + " suspend account", suspend_user_account_admin_user_path(admin), data: {confirm: "Are you sure you want to suspend #{admin.first_name}'s account?"}, method: :patch, class: "buy_now" %>
                                  </div>
                                  </td>
                              </tr>
                              <% end %>
                              
                                                            
                           </tbody>
                        </table>
                      </section>
                  </div>
              </div>
              <!-- page end-->