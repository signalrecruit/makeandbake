              <div class="row">
                  <div class="col-lg-12">
                      <section class="panel">
                          <header class="panel-heading">
                              <%= pluralize(@suspended_users.count, "suspended account") %>
                          </header>
                          
                          <table class="table table-striped table-advance table-hover">
                           <tbody>
                              <tr>
                                 <th><i class="fa fa-user" aria-hidden="true"></i>
 Full Name</th>
                                 <th><i class="fa fa-envelope" aria-hidden="true"></i>
</i> Email</th>
                                 <th><i class="icon_pin_alt"></i> user's products</th>
                                 <th><i class="fa fa-mobile" aria-hidden="true"></i>
Phonenumber</th>
                                 <th><i class="icon_cogs"></i> Actions</th>
                              </tr>

                              <% @suspended_users.each do |user| %>
                              <tr>
                                 <td><%= link_to "#{user.first_name}, #{user.last_name}", [:admin, user] %></td>
                                 <td><%= user.email %></td>
                                 <td><%= link_to "#{user.first_name}'s products", admin_products_of_user_path(user) %> (<%= pluralize(user.products.count, "product") %>)</td>
                                 <td><%= user.phonenumber %></td>
                                 <td>
                                  <div class="btn-group">
                                      <%= link_to fa_icon("pencil") + " Edit", [:edit, :admin, user], class: "buy_now" %>
                                      <%= link_to fa_icon("trash") + " Delete", [:admin, user], method: :delete, data: {confirm: "Are you sure you want to delete this user?" }, class: "buy_now" %> <%= link_to fa_icon("archive") + " unsuspend", reverse_user_suspension_admin_user_path(user), data: {confirm: "Are you sure you want to unsuspend #{user.first_name}'s account?"}, method: :patch, class: "buy_now" %>

                                  </div>
                                  </td>
                              </tr>
                              <% end %>
                              
                                                            
                           </tbody>
                        </table>
                      </section>
                  </div>
              </div>
              <!-- page end-->